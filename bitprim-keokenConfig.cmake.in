# - Config file for the BITPRIM_KEOKEN package
#
#  BITPRIM_KEOKEN_INCLUDE_DIRS - include directories for BITPRIM_KEOKEN
#  BITPRIM_KEOKEN_LIBRARIES    - libraries to link against

get_filename_component(BITPRIM_KEOKEN_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(BITPRIM_KEOKEN_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

include("${BITPRIM_KEOKEN_CMAKE_DIR}/bitprim-keokenTargets.cmake")
set(BITPRIM_KEOKEN_LIBRARIES bitprim-keoken)

find_package(bitprim-blockchain 0 REQUIRED)

